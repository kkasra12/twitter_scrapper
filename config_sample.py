# please rename this file to config.py
# and fill in the connection info
connection_info = {
    "host": "your host",
    # if you are using a local database, use "localhost"
    "dbname": "your database name",
    # the name of the database you created -> "CREATE DATABASE name;"
    "user": "your username",
    # the username you used to create the database -> "CREATE USER the_user_name WITH PASSWORD 'password';"
    "password": "your password"
    # the password you used to create the database -> "Alter USER the_user_name with password 'password';"
}
# the bearer token you got from twitter
bearer_token = "your bearer token - mostly a long string of characters and numbers"
